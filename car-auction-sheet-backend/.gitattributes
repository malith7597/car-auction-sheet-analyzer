# Normalize line endings: LF in the repo, auto on checkout
* text=auto eol=lf

# Windows batch files must stay CRLF
*.bat text eol=crlf

# The Gradle wrapper script must be LF so it runs on Linux/CI
gradlew text eol=lf

# Binary assets
*.jar binary
